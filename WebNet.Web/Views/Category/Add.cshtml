@using System.Data
@using WebNet.DAL
@model WebNet.Model.Category
@{
    ViewBag.Title = Model.Id == 0 ? "新增版块 " : "编辑类目";
    Layout="_Layout";
}
<div class="layui-tab layui-tab-brief" lay-filter="docDemoTabBrief">
  <ul class="layui-tab-title">
    <li class="layui-this">@ViewBag.Title</li>
  </ul> 
</div> 
<form class="layui-form" action="">

    <div class='layui-form'>
        <div class='layui-form-item'> 
            <label class='layui-form-label'>板块名称</label> 
            <div class='layui-input-block'> 
                @Html.TextBoxFor(a => a.Caname, new { @class = "layui-input",lay_verify = "required" }) 
            </div> 
            <div class='layui-form-mid layui-word-aux'></div> 
        </div> 
        <div class='layui-form-item'> 
            <label class='layui-form-label'>编号</label> 
            <div class='layui-input-block'> 
                @Html.TextBoxFor(a => a.Bh, new { @class = "layui-input",lay_verify = "required" }) 
            </div> 
            <div class='layui-form-mid layui-word-aux'></div> 
        </div> 
        <div class='layui-form-item'> 
            <label class='layui-form-label'>父编号</label> 
            <div class='layui-input-block'>
                 @Html.TextBoxFor(a => a.Pbh, new { @class = "layui-input",lay_verify = "required" })  
            </div> 
            <div class='layui-form-mid layui-word-aux'></div> 
        </div> 


        
        <div class="layui-form-item">
 <label class='layui-form-label'>&nbsp;</label>
            <div class="layui-input-inline">
                <button id="btn1" class="layui-btn" lay-submit="" lay-filter="demo1">@ViewBag.Title</button> 
                @Html.AntiForgeryToken()
                @Html.HiddenFor(a=>a.Id)
            </div>
 <div class="layui-input-inline">
                <button class="layui-btn layui-btn-primary" type="button" onclick="history.go(-1)">返回</button>
            </div>
        </div>
    </div>

    </form>
<script>

var pop_index;
    var editor1;

        layui.use(['form',  'layer','laydate','upload'], function () {
            var form = layui.form;
var laydate = layui.laydate;
  var upload = layui.upload;
  var layer = layui.layer;
  var $ = layui.jquery;
 

 var uploadInst = upload.render({
				elem:'#btnimg',
                url: '/Home/ImgUpload' 
                , done: function (res) { 
                    document.getElementById("img1").src = res.data.src;
                    document.getElementById("img").value = res.data.src;   
                }
            });

 


            form.on('submit(demo1)', function (data) {
                var url = "/Category/add";
                var postdata = data.field; 
			

        $('#btn1').attr("class", "layui-btn layui-btn-disabled");
            $('#btn1').attr("disabled", "disabled");

                pop_index = layer.load(0, {
                shade: [0.3, '#000'] 
            });  
                $.post(url, postdata, function (data) {
layer.close(pop_index);
                    if (data.code == 0) {
                        layer.msg(data.msg, function () { location.href = "/Category/";})
                    } else {
                        layer.alert(data.msg);
$('#btn1').attr("class", "layui-btn");
            $('#btn1').removeAttr("disabled");
                    }
                }, 'json')
                return false;
            });

        });
    </script>
